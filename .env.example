# Supabase project connection. No project is provisioned yet for this
# foundation — copy this file to .env.local and fill in values once one
# exists. The app must run with these unset; features that need Supabase
# should report themselves as unavailable, not fail silently or fake data.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
